# app.profile, k8s.namespace e image.tag definidos via jenkins
app:
  name: api-painel
  profile:
k8s:
  namespace:
image:
  repository: deltacare/api-painel 
  tag:
service:
  type: ClusterIP
  port: 8080
