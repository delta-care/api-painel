server:
  port: 8080
spring:
  application:
    name: api-painel
#  profiles:
#    active: dev
  cloud:
    kubernetes:
      config:
        name: api-painel
        enabled: true
      reload:
        enabled: true